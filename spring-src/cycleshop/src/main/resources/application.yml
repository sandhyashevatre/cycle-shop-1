spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cycle-data
    username: saisidharthak
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

 

jwt:
  private:
    key: classpath:app.key
  public:
    key: classpath:app.pub